{
  "name": "pwa-polymer-crud-todo-app",
  "version": "0.0.0",
  "description": "Polymer ToDo Crud App for wk3 of PWA class",
  "license": "BSD-3-Clause",
  "scripts": {
    "lint": "eslint . --ext js,html --ignore-path .gitignore",
    "test": "npm run lint && polymer test",
    "build": "gulp build"
  },
  "devDependencies": {
    "del": "^2.2.0",
    "eslint": "^3.12.0",
    "eslint-config-google": "^0.7.0",
    "eslint-plugin-html": "^1.7.0",
    "gulp": "gulpjs/gulp#4.0",
    "gulp-html-minifier": "^0.1.8",
    "gulp-if": "^2.0.0",
    "gulp-imagemin": "^3.1.0",
    "merge-stream": "^1.0.0",
    "polymer-build": "^0.6.0"
  },
  "engines": {
    "node": ">=4.0"
  },
  "private": true
}
